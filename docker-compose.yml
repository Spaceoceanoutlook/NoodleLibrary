services:
  db:
    image: postgres:15
    environment:
      POSTGRES_DB: noodlesdb
      POSTGRES_USER: valerii
      POSTGRES_PASSWORD: 456321
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data:
